{% extends 'base.html.twig' %}

{% block title %}
	{{ project.name }}
{% endblock %}

{% block body %}
	<h1>{{ project.name }}</h1>
	<p>Zichtbaarheid:
		<small>
			{% set visibilityLabels = {0: 'Privé', 1: 'Intern', 2: 'Mentor'} %}
			{{ visibilityLabels[project.visibility] ?? 'Onbekend' }}
		</small>
	</p>
	<div class="mt-4">
		<a href="{{ path('project_edit', {'id': project.id}) }}" class="btn btn-warning">Bewerken</a>
		<a href="{{ path('project_index') }}" class="btn btn-secondary">Terug</a>
	</div>
{% endblock %}
